# Atlas — Next Actions

> Generated by atlas-indexer/1.0.0 (P0) at 2026-05-27T05:41:01.952546+00:00.

## Atlas roadmap (publish phases)

- **P2 ATLAS_GDRIVE_PUBLISHER** — mirror `public/` + gated handoffs to Drive `Ariadne Atlas/`. Status: NOT_IMPLEMENTED_BY_DESIGN.
- **P3 ATLAS_GITHUB_POINTER_PUBLISHER** — push pointer/index layer to `context/atlas/`. Status: NOT_IMPLEMENTED_BY_DESIGN.
- **P4 ATLAS_SCHEDULED_PUBLISH** — gated scheduled refresh. Status: NOT_IMPLEMENTED_BY_DESIGN.
- **P5 ATLAS_WINDOW_INTEGRATION** — expose Atlas pointer via Window read surface. Status: NOT_IMPLEMENTED_BY_DESIGN.

## Latest run's recommendation

From `harness/runs/ATLAS_P0_CONTRACT_AND_LOCAL_INDEXER/NEXT_PACKET_RECOMMENDATION.md`:

```
# NEXT_PACKET_RECOMMENDATION.md — ATLAS_P0_CONTRACT_AND_LOCAL_INDEXER

## Decision

Local indexer **PASSED** (build exit 0, all indexes non-empty and valid).

➡️ **Recommended next packet: `ATLAS_P2_GDRIVE_PUBLISHER`**

(If the local indexer had been BLOCKED, the fallback would be
`ATLAS_P1_LOCAL_INDEXER_REPAIR`. It is not needed.)

## ATLAS_P2_GDRIVE_PUBLISHER — scope preview

- Mirror `operator/atlas/public/` outputs to Google Drive `Ariadne Atlas/` per
  `ATLAS_GDRIVE_LAYOUT.md`.
- Add a size/age-gated handoff-zip + selected report/contract publish set.
- Introduce OAuth + folder id in `config/atlas_gdrive.yaml` (flip `enabled: true`)
  under explicit user approval; share-link creation is a separate explicit approval.
- New writer: `operator/atlas/scripts/atlas_publish_gdrive.py` (`-DryRun` default).
- Validation: dry-run upload plan; idempotent re-run no-op; no excluded class
  uploaded; redacted logs.

## Optional parallel

- `ATLAS_P3_GITHUB_POINTER_PUBLISHER` (pointer/index-only
```
